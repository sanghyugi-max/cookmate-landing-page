# 실제 자료를 넣는 폴더

다음 파일명은 예시입니다. 실제 파일은 제공된 자료가 없어 포함하지 않았습니다.

- `demo.mp4`: 실제 제품 작동 영상 (권장: H.264 영상 + AAC 음성)
- `poster.jpg`: 영상 재생 전 표시할 실제 사진
- `case-01.jpg`: 게시 동의를 받은 설치 매장 사진
- `captions.vtt`: 한국어 자막 (선택)

파일을 이 폴더에 넣은 뒤 `config.js`에서 `assets/파일명`으로 연결하세요.
대소문자와 확장자까지 실제 파일 이름과 동일해야 합니다.
